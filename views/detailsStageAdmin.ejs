<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Stage Express</title>
    <link rel="stylesheet" href="/css/11.detalisStageAdmin.css" />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat+Alternates:wght@400;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <header>
      <nav>
        <div id="divNav">
          <div id="titreNav">
            <a href="/admin2"><h1>Stage Express</h1></a>
          </div>
        </div>
      </nav>
    </header>
    <main>
      <div id="divBody">
        <div class="container">
          <div class="form_area">
            <h2>Page de vérification des détails de l'administrateur</h2>
            <div class="form_group" id="big">
              <div class="form_area" id="first">
                <p class="pFirst">
                  Titre du stage :<%= annonce. titreDeStage %>
                </p>
                <p class="pFirst">
                  Durée du stage :<%= annonce.dureeDuStage %>
                </p>
                <p class="pFirst">Domaine du stage :<%= annonce.domaine %></p>
                <p class="pFirst">Date de début :<%= annonce.dateDeDebut %></p>
                <p class="pFirst">Lieu du stage :<%= annonce.lieuDuStage %></p>
              </div>
              <div class="form_area" id="seconds">
                <p id="pSeconds">
                  Description du stage : <br /><%= annonce.description %>
                </p>
              </div>
              <div class="form_area" id="circul">
                <div id="logoE">
                  <img
                    src="http://localhost:3005/<%= annonce.logo %>"
                    alt="Logo Entreprise"
                  />
                </div>              </div>
            </div>
            <div id="btns">
                <form
                action="/approve-annonce/<%= annonce._id %>"
                method="POST"
                style="display: inline"
              >
                <button id="accepter" type="submit" class="btn-accept">Accepter</button>
              </form>
              <form
              action="/reject-annonce/<%= annonce._id %>?_method=delete"
              method="POST"
                style="display: inline"
              >
                <button id="refuse" type="submit" class="btn-refuse">Refuser</button>
              </form>
              </div>
          </div>
         
        </div>
        
      </div>
      
    </main>
    
  </body>
</html>
